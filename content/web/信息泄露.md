---
draft: true
---
### 0x1 信息泄露的类型

文件泄露，根据泄露的信息敏感程度，在web漏洞中可以算中危甚至高危的漏洞

主要分为由版本管理软件导致的泄露，文件包含导致的泄露和配置错误导致的泄露

![](111.png)

上面的只是前人总结出来的突破点，需要灵活运用，不能过于呆板。比如这个敏感文件泄露，有的师傅会问：访问http://xxx/www.zip 404怎么办？404就是此路不通，需要重新找思路。

我们的目的不是秀操作，而是真真实实拿到我们需要的敏感文件，至于从哪个方式，哪个路径拿到的，其实不重要。不管黑猫白猫抓住老鼠就是好猫

上面的图只是说明有这些可能，但是不一定信息泄露一定就在上面的这几种情况之中。

### 0x2 信息泄露漏洞利用

##### 1 http头信息泄露

这里我们需要区别下`/admin/`和`/admin` 的区别，虽然两者只相差一个斜杠，但是访问的是不同的文件。

`/admin/`表示访问的是admin目录，会默认访问目录里的索引文件，如index.html或index.php

`/admin`表示访问admin文件，如果没有这个文件，就直接返回404，不会去访问敏感文件

有些情况下加不加斜杠都能访问到`/admin/`，这是因为配置文件里进行了设置，这里我们后面会讲。

这里还要区别的是路由，目前网站主流都是基于路由的，比如：

`/user` 这里则表示访问的是user路由，而不是访问user这个文件，需要根据server头来推断，比如php,asp,hsp,aspx,html,shtml这些后缀，是基于文件的。

而那些看起来没有后缀，或者类似于`/user/login?username=admin&password=123`这种形式，则是基于路由的，这个需要多次判断

- 200 表示正常
- 301 302表示跳转
- 404表示文件不存在
- 500表示服务器内部错误
- 502表示转发失败 error gateway

路由的特点 所见非真 哪怕文件+后缀名访问到了也不能够确定就是文件访问
##### 2 报错信息泄露

##### 3 页面信息泄露

##### 4 robots.txt文件泄露

例题 web1

进入robots.txt，看到disallowed

![](web1_1.png)

泄露了后台登陆地址的敏感地址，访问该地址：

![](web1_2.png)
### 5.git文件泄露

githack工具

没什么好说的

### 6.搜索引擎收录泄露

google hack

### 7.fofa资产搜索

## 0x3 信息泄露总结

水无常形 招无定式

不能拘泥于上面的几种情况，在渗透实战中会有其他更多的姿势。但是目的都是一样的：尽一切可能多地搜集对面的信息，收集得越多越好

在CTF比赛中，信息泄露考点难度普遍较低，不会隐藏特别深，一般有以下几种：

1. robots.txt泄露（里面有可疑路径或敏感文件名称）
2. git/svn泄露(dirsearch可以直接扫)
3. 页面注释泄露（右键查看源代码），http头泄露（类似百度的BWS/1.,1）
4. 任意文件下载造成的信息泄露（可以下载WEB-INF文件夹里内容）
5. 开源社区信息泄露，例如：泄露出项目名称，而整个项目代码可以在github，gitee等找到
6. 使用vim异常退出，可能存在类似.index.php.swp临时文件

还有一部分比较冷门的信息泄露，比如.DS_Store泄露，nginx错误配置导致的文件泄露

