---
draft: true
---
- 按字节编址：每个存储单元大小为1字节（8个bit）
- 按字长编址：每个存储单元大小为1个字（取决于特定计算机字长长度）

### 内存管理

- 操作系统负责内存空间的分配与回收
	- 操作系统如何记录已经被分配和空闲的内存？
	- 操作系统如何回收结束运行的进程的内存空间？
	- 操作系统如何分配内存给进程？
- 操作系统需要提供某种技术从逻辑上对内存空间进行扩充
- 操作系统负责地址转换（从逻辑地址到物理地址）
- 操作系统需要提供内存保护技术（保证各自在各自的内存空间中运行）

##### 内存保护

内存保护可采取两种方法：
1. 在CPU中设置一对上下限寄存器，存放进程的上下限地址。进程的指令要访问某个地址时，检查是否越界
2. 采用重定位寄存器（又称基址寄存器）、和界地址寄存器（又称限长寄存器）进行越界检查。重定位寄存器中存放进程的**起始物理地址**，界地址寄存器中存放进程的**最大逻辑地址**（看作长度）。

##### 进程的内存映像

在32位系统中，进程虚拟地址空间为4GB

- 0xC000 0000 ~ 0xFFFF FFFF存放内核代码，内核数据结构等，是操作系统内核区
- 0x0804 8000 ~ 0xC000 0000是user space
- 0x0000 0000 ~ 0x0804 8000是未使用区

虚拟地址空间只是映射的结果。假设内存中有多个进程，那么这n个进程的内核区都会指向实际物理内存中的同一片区域

在user space中，从底部到顶部，又有：
- 只读代码/读数据 —— 程序指令、只读数据
- 读/写数据 —— 全局变量、静态变量
- 堆 heap 最多从下到上到0x4000 0000
- 共享库映射区 (libc.so)
- 用户栈 Stack

##### 内存空间的扩充：覆盖、交换

- 覆盖

用以解决“程序大小超过物理内存总和”问题

思想：将程序分为多个段，常用段驻守内存，非常用段只在需要时调入内存

已退出历史舞台

- 交换

思想：在内存空间紧张时，将某些进程暂时换出到外存，将外存中具备运行条件的进程换入内存（进程在内存与磁盘间动态调度）

暂时换出外存等待的进程状态成为挂起态 suspend 挂起态又可以进一步细分为就绪挂起、阻塞挂起

##### 内存空间的分配与v回收

连续分配管理方式：（连续分配：指为进程分配的是一个连续的内存空间）

- 单一连续分配
	- 内存被分为系统区、用户区。用户区此时只能有一道程序，也就是说**不支持多道**
- 固定分区分配
	- 
- 动态分区分配

非连续分配管理方式














